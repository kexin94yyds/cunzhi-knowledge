## R-2026-025: VSCode 插件服务启动回归检查
- **P-ID**: P-2026-025
- **检查内容**:
    1. **服务启动**: Reload Window 后，是否显示 "iterate 服务已启动，端口: XXXX"
    2. **端口可用**: `lsof -i :端口号` 是否显示 iterate 进程在监听
    3. **脚本调用**: `python3 cunzhi.py 端口号` 是否返回 `KeepGoing=true`
    4. **终端输出**: 调用脚本时终端是否干净（无日志污染）
    5. **图片路径**: `input.md` 中是否正确显示 `image_paths:` 行
- **状态**: verified

---

## R-2026-001: 爱特文件 UI 优化回归检查
- **P-ID**: P-2026-001
- **检查内容**:
    1. **文本验证**: 按钮文本是否显示为 "➕ @文件"
    2. **弹出位置**: 点击按钮后，菜单是否向下弹出 (top-full)
    3. **自动聚焦**: 点击按钮后，搜索框是否自动聚焦
    4. **模拟触发**: 在主输入框输入 "@" 或 "爱特"，是否自动弹出文件搜索菜单
- **状态**: verified
